@using System;
@using Sandbox.UI;
@using KOTH.PlayerExp;

@namespace KOTH.UI
@inherits Panel

@attribute [StyleSheet]

<root class="">
@* 	@if (false)
	{
		<div onclick="@(() => { Game.IsPaused = false; })">Resume</div>
		<div @onclick="@(() => Game.Overlay.ShowSettingsModal())">Settings</div>
		<div onclick="@(() => GoToMenu())">Main Menu</div>
	} *@
</root>

@code
{
	private void GoToMenu()
	{
		Game.IsPaused = false;
		Networking.Disconnect();
		Game.ActiveScene.LoadFromFile("scenes/menu/menu.scene");
	}

	private bool IsActive { get; set; }

	protected override int BuildHash()
	{
		return HashCode.Combine(Time.Now);
	}

	public override void Tick()
	{
		// IsActive = Game.IsPaused;
		// SetClass("visible", Game.IsPaused);
		// Log.Info(IsActive);
	}
}
