@using Sandbox;
@using Sandbox.UI;

@inherits Panel
@implements INavigatorPage
@namespace KOTH.UI

<root class="hc1">
    <a style="position: relative; left: 20%; top: 20%;">made by a single man (who doesn't understand how nav panels work)</a>
</root>

@code
{
    protected override void OnAfterTreeRender(bool firstTime)
    {
        if (firstTime)
        { }

        StateHasChanged();
    }
}
